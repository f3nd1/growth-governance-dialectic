import PageHeader from "../components/PageHeader"

export default function ExportCentre() {
  return (
    <>
      <PageHeader title="Export Centre" desc="Export the workspace as JSON and the Pilot Report as Markdown/HTML, always carrying the SYNTHETIC caveat." />
      <div className="card muted">This page is built in a later phase.</div>
    </>
  )
}
