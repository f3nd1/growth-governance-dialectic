import PageHeader from "../components/PageHeader"

export default function StudyDesign() {
  return (
    <>
      <PageHeader title="Study Design" desc="Editable summary of the convergent mixed-methods single-case design — the read-only reference the rest of the app points back to." />
      <div className="card muted">This page is built in a later phase.</div>
    </>
  )
}
